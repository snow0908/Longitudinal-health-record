# 2026-09-11

## 隐私保险箱原型 · 编辑页去掉「医生」列
- 文件：`02_产品设计/02_原型设计/健康全景视图1.4/个人隐私管理/隐私保险箱.html`
  （注意：项目内路径实际是 `02_产品设计/02_原型设计/...`，不是 `02_原型设计/...`）
- 编辑页模块清单的表头与行都由 JS 渲染，医生列共涉及 4 处：
  1. `renderModules()` 中表头 `<span class="th doc">医生</span>` — 已删除
  2. `recRow()` 中 `<span class="rec-col doc">' + r.doctor + '</span>` — 已删除
  3. CSS `.rec-col.doc { width: 80px; }` — 已删除
  4. CSS `.bd-table-hd .th.doc { width: 80px; }` — 已删除
- 列宽保持不变：内容名称 flex:1 / 就诊时间 180px / 医疗机构 220px，去掉末列后无对齐错位。
- DATA 中的 `doctor` 字段保留未动（仅不渲染），后续如需恢复成本最低。
- 只影响「编辑」页；查看页（`renderView()` → viewList）本来就没有医生列。

## 隐私保险箱原型 · 健康档案模块「就诊时间」→「服务时间」
- 同一文件 `隐私保险箱.html`。tab 的 group key：`diag`=诊疗服务，`health`=健康档案。
- `renderModules(group)` 内新增 `var timeText = group === 'health' ? '服务时间' : '就诊时间';`，筛选区与表头共用。
- 筛选区 label：health 用新增的 `.tb-label-plain`（纯文字，13px/#595959，无蓝底胶囊），diag 仍用原 `.tb-label`（蓝底胶囊 #E6F4FF/#2378F5）。
- 表头 `<span class="th date">` 文案跟随 timeText。诊疗服务侧文案与样式均未动。
- 页面内所有时间文案都集中在这一个函数里渲染，全站改时间文案只需改 `timeText` 一行。

### 追加：诊疗服务侧也去掉蓝底胶囊
- 应用户要求，诊疗服务的筛选区 label 一并改用 `.tb-label-plain`，现在筛选区文案统一为纯文字。
- 因已无任何元素引用，CSS 中的 `.tb-label` 规则已删除。如需恢复蓝底胶囊，规格为：
  `display:inline-flex; align-items:center; height:28px; padding:0 12px; background:#E6F4FF; color:#2378F5; border-radius:14px; font-size:12px; line-height:28px;`
- 当前筛选区 label：`.tb-label-plain`（13px / #595959 / height 28px）；两个 tab 唯一差异只剩文案（就诊时间 / 服务时间）。


