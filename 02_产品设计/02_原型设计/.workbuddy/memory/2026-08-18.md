# 2026-08-18 工作记录

## 病案首页详情（全程档案调阅系统 v1.1）

### 第一轮：tab 重构 + 报告列表
- tab 由「基本信息 / 诊断信息 / 手术操作」改为「就诊信息 / 检查报告 / 检验报告」
- 原诊断 + 手术内容合并到"就诊信息" tab，作为子卡片（`诊断信息` + `手术 / 操作`）
- 新增 `.report-card` 卡片列表样式（16px 中等字重名称 / 14px 时间 / 14px 灰色医院行 + 小图标），实现检查报告与检验报告两个 tab
- RECORDS 每条记录新增 exams、labs 数组

### 第二轮：tab pill 样式 + 用药信息
- tab 由"等宽卡片分隔"改为 pill 胶囊：active 为蓝底白字（#2378F5）+ 阴影，inactive 为透明灰字；`.tab-bar` 去掉卡片底与边框，tab 之间用 8px gap
- 在"就诊信息" tab 内新增"用药信息"子卡片：父卡片标题 `用药信息 · 共 N 条`，内部多个 `.meds-item` 子条目（药品名 + 蓝色用法 + 时间 + 分割线 + 单次剂量/用药频率/用药天数/总剂量 两列网格 + 药物剂型 跨满）
- RECORDS 4 条记录补充 meds 数组：
  - 急性肠胃炎：阿莫西林胶囊、蒙脱石散、口服补液盐Ⅲ
  - 高血压复诊：苯磺酸氨氯地平片、阿司匹林肠溶片
  - 2 型糖尿病：盐酸二甲双胍片、格列美脲片
  - 急性心肌梗死：阿司匹林、硫酸氢氯吡格雷、阿托伐他汀钙、硝酸甘油
- 新增 renderMedsSection(r) 渲染函数，在 renderBasicPane 中插入到 诊断/手术 卡片之后

### 第三轮：导航栏 ·|·⊙ + 摘要卡片按状态变主题
- 导航栏右上角：移除原 `../img/设置.png` 图片按钮，改为 `<div class="nav-right">` 三元素结构：
  - `.nav-dots`（两个圆点 `::` 文字，scaleY 1.4 拉长，深色）
  - `.nav-divider`（浅灰竖线 `|`）
  - `.nav-circle`（24×24 圆 + 4×4 中心圆点，深色边框）
- 摘要卡片（`.card-highlight`）按 `r.status` 动态切换主题色（4 套配色）：
  - `card-highlight--pending` 暖黄 #FFFBEB 底 / #FEA800 边
  - `card-highlight--cancel` 灰 #F5F5F5 底 / #CCCCCC 边
  - `card-highlight--success` 绿 #EFFAF5 底 / #18CF8E 边
  - `card-highlight--danger` 红 #FFF4F4 底 / #F32929 边
- 摘要卡片右上角标签从 `医保类型（hdPayment，蓝 info）` 改为 `状态（hdStatus，hd-state-tag 风格跟随 hero 主题色）`
- 医保信息保留在"就诊信息 → 费用信息"子组（门急诊总费用 / 自付金额 / 医保支付）—— 字段无丢失
- JS `initDetail`：`heroEl.className = 'card-highlight card-highlight--' + r.status`，并把 `r.statusText` 写入 `#hdStatus`
- 4 条示例记录状态：id1 待就诊(pending) / id2 已退号(cancel) / id3 已就诊(success) / id4 已转入住院(danger)

### 第六轮：诊断/手术信息展示优化（内容字体保持 14px）
- 弱化"卡片套卡片"厚重感：`.diag-card` 边框由 `#E5E5E5` 改为浅灰 `#EEF1F4`，padding 16→14，加 `position:relative; overflow:hidden`
- 主诊断/主手术突出：`.diag-card.primary` 浅蓝底 `#F5F9FF` + 左边 3px 主题色 `#2378F5` 强调条（`::before`）；`renderOpCards` 改为按 `o.primary` 加 `primary` 类，与诊断一致
- 中医诊断区分：`.diag-card.tcm` 浅暖底 `#FBFAF6` + `#F0E9D6` 边
- 间距优化：tags gap 8→6、margin-top 10→8；detail grid gap 8→10 行距；field-v `margin-top` 2→3
- 字号约束：字段值 `.field-v` 保持 14px（field-k 12px 标签、diag-card-name 16px 标题不变），满足"内容字体保持 14px"

### 第五轮：tab 高度对齐设计稿 + 摘要卡片满宽去 border
- 对齐 Figma 设计规格：
  - `.tab-item` `height:30px; line-height:30px; padding:0; border-radius:15px`（30pt 高、15pt 圆角的真胶囊）
  - `.tab-bar` `gap:7px`（与设计稿 7pt 一致）
  - `.tab-bar.in-hero` `margin:8px 16px 0 16px; padding:0; border:none`（去掉之前的 16px 顶距与 1px 分割线，与设计稿更紧凑）
- 摘要卡片去外框：
  - `.hero-card` 改为 `margin:0; width:375px; border:none; background-color:transparent; padding:0` —— 满宽 375、无 border、透明底，作为隐形容器
  - `.card-highlight` 补 `margin:0 16px` 让彩色状态边框离页面两侧 16px
  - 视觉上：导航 → 状态彩色卡片 → tab → 内容卡片，全部 375px 满宽，尺寸 token 统一

### 第四轮：修复摘要不展示 BUG + 布局收敛（tab 嵌入卡片底部、卡片贴顶）
- **BUG 根因**：右上角按钮之前仍是旧 `<img src="../img/设置.png">`，但底部 JS 绑定了 `getElementById('btnMore')` 与 `getElementById('btnCircle')` —— 两者在 HTML 中不存在 → 返回 null → `.addEventListener` 抛 TypeError → 脚本中止 → `initDetail()` 从未执行 → 摘要信息永久停在占位「—」。
- **修复**：把 `nav-right` 改为真正带 id 的三元素（`#btnMore` 圆点 / `.nav-divider` 竖线 / `#btnCircle` 圆圈带点），与第三轮的 `.nav-dots/.nav-divider/.nav-circle` 样式对应；事件绑定不再抛错，`initDetail()` 正常渲染摘要。
- **布局调整**：
  - 摘要卡片加 `.hero-card { margin-top:0 }` → 紧贴顶部导航，不再作为独立留白浮卡。
  - 胶囊 `.tab-bar` 由卡片外独立元素**移入摘要卡片内部底部**（新增 `.tab-bar.in-hero`：`margin:16px 0 0 0; padding-top:16px; border-top:1px solid #EEF0F3`），与摘要卡片共用 16px 内边距，尺寸统一。
  - 卡片顺序保持：导航 → 摘要卡片(含 tab) → 内容面板。

### 第七轮：诊断/手术/用药模块整体布局重构（去套娃 + 统一视觉语言）
- **问题**：三块均为「外层 `.card`(灰边框) + 内层条目卡(又一层边框)」套娃，且诊断/手术用 `.diag-card`、用药 `.meds-card` 两套不同语言，与白色无框摘要卡割裂；用药字段 key 用 14px 黑字（与其它 12px 灰不一致）。
- **重构为新体系**：
  - 新增统一分组标题 `.section-head`：左侧语义色竖条(`.bar`) + 15px/600 标题 + 右侧 12px 灰色计数；诊断=蓝 `#2378F5`、手术=紫 `#7C5CFC`、用药=绿 `#18A058`
  - 诊断/手术条目改用 `.info-item`：页面级白色圆角卡(10px)、极浅边 `#EEF1F4`、条目间距 10px；主条目蓝底+左蓝条(`.primary`)、中医暖底(`.tcm`)；字段值 `.field-v` 保持 14px、key 12px 灰
  - 用药改用 `.meds-wrap` 扁平父卡(所有药品合并一卡、内部 `.meds-item` 用 `#F0F2F5` 分隔线)；用法改为绿色胶囊标签 `#EAF7F0` 底 `#18A058` 字；字段 key 统一 12px 灰(原 14px 黑)
  - JS：`renderDiagCards/renderOpCards` 类名 diag→info；`renderBasicPane` 诊断/手术去掉外层 `.card` 改为 `section-head`+直接列表；`renderMedsSection` 改为 `section-head.drug`+`meds-wrap`
- 结果：三块与白色无框摘要卡语言统一、扁平无套娃、语义色区分模块，内容字号保持 14px

### 第九轮：患者信息/费用信息布局调整
- **患者姓名上移摘要卡片**：
  - 给每条 RECORD 新增 `name`/`gender` 字段（原 BASIC_GROUPS 中「张自强」对所有记录共享显示，已不合理）；4 条示例分别：张自强(男)/李建国(男)/王秀兰(女)/赵国强(男)
  - 摘要卡片 `.card-highlight` 顶部新增 `.hero-patient`（`.hero-name` 18px/600 + `.hero-gender` 13px 灰「· 男」），位于诊断名称之上；`initDetail` 写入 `#hdName`/`#hdGender`
- **删除「患者信息」卡片**：从 BASIC_GROUPS 移除 `name:'患者信息'` 分组（门诊号/病案号/身份证/电话/地址等字段随之从 UI 移除，符合"去掉患者信息卡片"要求）
- **费用信息卡片置底**：
  - 从 BASIC_GROUPS 移除「费用信息」分组
  - 新增 `renderFeeSection(r)` + `fmtMoney(n)`：`门急诊总费用/自付金额/医保支付` 按当前记录 `totalFee/selfFee` 计算（医保支付=总费用-自付），复用 `renderBasicGroup` 卡片样式
  - `renderBasicPane` 返回顺序：`groupsHtml(就诊信息/主诉/现病史/既往史/过敏史) + 诊断 + 手术 + 用药 + 费用信息 + 提示条` —— 费用信息卡现为最后一张内容卡
- 注：摘要卡片内原有的「总费用/自费」紧凑行仍保留作为摘要概览，与底部详尽费用卡并存

### 设计 token
- 主题色 `#2378F5`，费用高亮 `#F32929`，药品/用药语义色 `#18A058`，手术语义色 `#7C5CFC`
- 主色 box-shadow `0 4px 12px rgba(35, 120, 245, 0.25)` 用于 pill 激活态
- 状态色：warning #FEA800 / success #18CF8E / danger #F32929 / cancel #CCCCCC
- 扁平卡边 `#EEF1F4`、分隔线 `#F0F2F5`、分组标题 15px/600、条目名 15px/600、字段值 14px、字段 key 12px

### 第八轮：诊断/手术样式对齐用药（合并父卡 + 分隔线 + 状态胶囊）
- 用户要求诊断、手术样式"找用药的效果展示"——与用药模块统一
- 诊断/手术由"各条目独立带边框圆角卡"改为与用药一致：所有条目合并在扁平父卡 `.info-wrap`（10px 圆角、`#EEF1F4` 极浅边、`overflow:hidden`）内，条目 `.info-item` 用 `#F0F2F5` 分隔线区分（`.info-item + .info-item`），无独立边框
- 头部标签由多标签行改为右上角状态胶囊 `.info-item-badge`（主诊断/主手术=蓝 `#EAF2FF`、中医=暖 `#FBF3E2`、其他/副手术=灰 `#F2F4F7`），视觉同用药用法胶囊
- 类型名/编码/手术编码/编码体系由原标签行移入字段网格，与用药字段网格一致；去掉"是否主诊/排序"冗余行
- 主诊断/主手术保留左侧蓝条 `.info-item.primary::before` + 浅蓝底；中医保留浅暖底
- JS：renderBasicPane 诊断/手术 section 包 `<div class="info-wrap">`；renderDiagCards/renderOpCards 用 badge 替代 tagHtml

### 第九轮：诊断信息卡片与手术/用药完全一致
- 用户要求"诊断信息展示效果和手术信息、用药信息保持一致"
- 标题：由 `.section-head`(语义色竖条+计数) 改为 `.card` + `.simple-card-title`「诊断信息」，与手术「手术信息」/用药「用药信息」同构；去掉计数与西医/中医拆分文案
- 去嵌套：移除内层 `.info-wrap` 边框卡，诊断条目直接平铺在 `.card` 内，用 `.info-item + .info-item` 的 `#F0F2F5` 分隔线区分（作用域 `.card > .info-item` padding 14px 0、按压满铺高亮，与手术一致）
- 字段布局：诊断字段由堆叠式 `.info-field`(标签在上值在下) 改为复用用药行内式 `.meds-field`(「标签 值」同行、key 12px 灰 / 值 12px 深灰)；中医证候 `syndRow` 改用 `.meds-field.full` 跨满整行
- 强调收敛：去掉主诊断左侧蓝条 `.info-item.primary::before` 与浅蓝底、去掉中医 `.info-item.tcm` 暖底；仅保留右上角状态胶囊(主诊断蓝/中医暖/其他灰)，对应用药的"用法胶囊"
- 清理死 CSS：删除 `.section-head` / `.info-wrap` / `.info-item.primary` / `.info-item.tcm` / `.info-field` 整套失效规则
- 最终三块(诊断/手术/用药)视觉完全统一：白卡 + 简单标题(16px/500) + 名称+状态胶囊头部(margin-bottom 12px) + 行内字段网格 + 分隔线

### 第十轮：用药/诊断/手术卡片内容字体统一 14px
- 用户要求三块卡片"里面的内容字体改成14px"
- `.meds-field .field-k`(标签) 由 12px 改为 14px(line-height 17→20)；`.meds-field .field-k .v`(值) 显式 14px
- 影响范围：`.meds-field` 被用药/诊断/手术三块共用，一次改动三块字段内容统一为 14px，与基础信息卡 `.simple-label`/`.simple-value` 一致
- 保留：条目名称 15px/600(作为子标题层级)、用法胶囊/状态胶囊 12px(标签化 tag 规范，同 `.tag` 12px)

### 第十一轮：手术信息卡片与用药信息卡片换位
- 用户要求"手术信息卡片和用药信息卡片换个位置"
- 仅改 `renderBasicPane` 拼接顺序：`diagSection + opSection + medsSection` → `diagSection + medsSection + opSection`
- 现"就诊信息"tab 内顺序：就诊信息 → 主诉 → 现病史 → 既往史 → 过敏史 → 诊断信息 → **用药信息** → **手术信息** → 费用信息 → 提示条（用药上移、手术下移）

### 第十二轮：首页 index.html 诊疗服务区重构 + 门诊记录跳转链路
- 去掉「就医记录/检查用药」切换图标与 JS（initMedToggle 已删），标题改为静态「诊疗服务」，两组卡片合并展示；后按新需求收敛为 6 张卡、2 排 ×3：住院记录 / 门诊记录 / 检查报告 / 检验报告 / 门诊用药 / 住院用药，删除「住院病案」「门诊病案」两张卡及其隐藏占位
- 门诊记录卡片 `onclick` 改为 `window.location.href='门诊记录/门诊记录.html'`（原 goToModule('门急诊记录')）
- `门诊记录/门诊记录.html` 列表三张卡片（.list-items_1-0/1/2）新增 <script>：`.list_1 > div` 绑定点击 → `门诊记录详情.html`
- 注：住院记录/检查报告/检验报告/门急诊用药/住院用药仍走 goToModule（无对应页时 toast 提示），保持原有行为

### 第十三轮：门诊用药卡片配色 + 病案首页摘要卡人群标签
- 首页 `index.html`：「门诊用药」卡片（`.group_5`，与「门诊记录」共用该类）改为行内 `background-color:#FFF4F4`（浅红），不影响其它卡片；`.group_5` 原浅绿底保留给「门诊记录」
- `病案首页/病案首页详情.html`：摘要卡「患者姓名」下方新增「人群标签」行（`.hero-tags` + `.pop-tag` 单字胶囊，灰色 #F2F4F7 底）
  - 每种业务精简成一字，胶囊 `title` 存全称：高=高血压、糖=糖尿病、核=肺结核、消=消化、心=心血管（示例沿用用户给的高/糖/核）
  - 4 条 RECORD 各加 `tags:[{c,n}]`：id1 消 / id2 高 / id3 糖+高 / id4 心+高+糖；`initDetail` 渲染 `#hdTags`，空时 `.hero-tags:empty` 隐藏不占间距
  - 仅改「病案首页详情」；若需同步到「门诊记录详情」摘要卡，需另行按该页数据模型实现
